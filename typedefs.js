//// *** TYPE DEFINITIONS ** ////
// * strnum
/**@typedef {string|number} strnum*/

// * TElemEvents
/**@typedef {
    {
        touchstart ? : (function (): * ),
         click ? : (function (): * ),
         animationend ? : (function (): * ),
         pointerdown ? : (function (): *),
    }
} TElemEvents*/


// * TElemOptions
/**@typedef {
 {
    tag?:('span'|'div'|'button'|'img'),
    id?: string,
    text?: string,
    htmlElement?: Element
 }
} TElemOptions*/


